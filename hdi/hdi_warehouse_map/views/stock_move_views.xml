<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Extend Stock Move Form View -->
    <record id="view_stock_move_form_inherit" model="ir.ui.view">
        <field name="name">stock.move.form.inherit</field>
        <field name="model">stock.move</field>
        <field name="inherit_id" ref="stock.view_move_form"/>
        <field name="arch" type="xml">
            <field name="location_id" position="after">
                <field name="source_bin_id"/>
            </field>
            <field name="location_dest_id" position="after">
                <field name="dest_bin_id"/>
                <field name="suggested_bin_id"/>
                <field name="selected_bin_id"/>
            </field>
        </field>
    </record>
    
    <!-- Extend Stock Move Tree View -->
    <record id="view_stock_move_tree_inherit" model="ir.ui.view">
        <field name="name">stock.move.tree.inherit</field>
        <field name="model">stock.move</field>
        <field name="inherit_id" ref="stock.view_move_tree"/>
        <field name="arch" type="xml">
            <field name="location_id" position="after">
                <field name="source_bin_id" optional="hide"/>
            </field>
            <field name="location_dest_id" position="after">
                <field name="dest_bin_id" optional="hide"/>
            </field>
        </field>
    </record>
</odoo>
