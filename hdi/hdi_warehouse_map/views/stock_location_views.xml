<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Extend Stock Location Form View -->
    <record id="view_location_form_inherit_warehouse_map" model="ir.ui.view">
        <field name="name">stock.location.form.inherit.warehouse.map</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet" position="before">
                <header>
                    <button name="action_configure_layout" string="⚙️ Configure Layout" 
                            type="object" class="btn-secondary"/>
                </header>
            </xpath>
            
            <xpath expr="//group[@name='localization']" position="after">
                <group string="Warehouse Map Configuration" name="warehouse_map">
                    <field name="location_type"/>
                    <field name="location_layout_ids" invisible="1"/>
                </group>
            </xpath>
        </field>
    </record>
    
</odoo>
