<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Tree View -->
    <record id="view_hr_payslip_tree" model="ir.ui.view">
        <field name="name">hr.payslip.tree</field>
        <field name="model">hr.payslip</field>
        <field name="arch" type="xml">
            <list string="Phiếu lương">
                <field name="employee_id"/>
                <field name="date_from"/>
                <field name="date_to"/>
                <field name="struct_id"/>
                <field name="state"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_hr_payslip_form" model="ir.ui.view">
        <field name="name">hr.payslip.form</field>
        <field name="model">hr.payslip</field>
        <field name="arch" type="xml">
            <form string="Phiếu lương">
                <header>
                    <!-- Bước 1: Tính lương -->
                    <button name="compute_sheet" string="Tính lương" type="object" class="oe_highlight" invisible="state != 'draft'"/>
                    
                    <!-- Bước 2: Gửi duyệt (draft → verify) -->
                    <button name="action_payslip_verify" string="Gửi duyệt" type="object" class="oe_highlight" invisible="state != 'draft'"/>
                    
                    <!-- Bước 3: Duyệt phiếu lương (verify → done) -->
                    <button name="action_payslip_done" string="Duyệt" type="object" class="oe_highlight" invisible="state != 'verify'"/>
                    
                    <!-- Bước 4: Đánh dấu đã thanh toán (done → paid) -->
                    <button name="action_payslip_paid" string="Đã thanh toán" type="object" class="oe_highlight" invisible="state != 'done'"/>
                    
                    <!-- Chuyển về nháp -->
                    <button name="action_payslip_draft" string="Chuyển về nháp" type="object" invisible="state not in ('cancel',)"/>
                    
                    <!-- Hủy phiếu -->
                    <button name="action_payslip_cancel" string="Hủy" type="object" invisible="state not in ('draft', 'verify')"/>
                    
                    <field name="state" widget="statusbar" statusbar_visible="draft,verify,done,paid"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="VD: Lương tháng 12/2025" readonly="state != 'draft'"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="employee_id" readonly="state != 'draft'"/>
                            <field name="contract_id" readonly="state != 'draft'" context="{'default_employee_id': employee_id}" domain="[('employee_id', '=', employee_id), ('state', 'in', ['open', 'close'])]"/>
                            <field name="struct_id" readonly="state != 'draft' or contract_id" options="{'no_create': True, 'no_open': True}"/>
                            <field name="is_probation" invisible="1"/>
                            <div class="alert alert-info" role="alert" invisible="not contract_id">
                                <div invisible="not is_probation">
                                    <strong>🔵 Trạng thái:</strong> Đang thử việc
                                    <br/>
                                    <small>Cấu trúc lương "Thử việc Việt Nam" được áp dụng tự động</small>
                                </div>
                                <div invisible="is_probation">
                                    <strong>✅ Trạng thái:</strong> Nhân viên chính thức
                                    <br/>
                                    <small>Cấu trúc lương "Nhân viên Việt Nam" được áp dụng tự động</small>
                                </div>
                            </div>
                        </group>
                        <group>
                            <field name="date_from" readonly="state != 'draft'"/>
                            <field name="date_to" readonly="state != 'draft'"/>
                            <field name="standard_days" readonly="state != 'draft'"/>
                        </group>
                    </group>
                    
                    <group>
                        <group string="Lương năng suất">
                            <field name="performance_wage_total" readonly="state != 'draft'" widget="monetary"/>
                            <div class="text-muted">
                                <small>💡 Nhập tổng lương NS → Hệ thống tự tính theo công thực tế</small>
                            </div>
                        </group>
                        <group>
                        </group>
                    </group>
                    <notebook>
                        <page string="Chi tiết lương" name="salary_lines">
                            <field name="line_ids">
                                <list editable="bottom">
                                    <field name="name"/>
                                    <field name="code"/>
                                    <field name="quantity"/>
                                    <field name="rate"/>
                                    <field name="amount"/>
                                    <field name="total" sum="Tổng cộng"/>
                                </list>
                            </field>
                        </page>
                        <page string="Ngày công">
                            <field name="worked_days_line_ids">
                                <list editable="bottom">
                                    <field name="name"/>
                                    <field name="code"/>
                                    <field name="number_of_days"/>
                                    <field name="number_of_hours"/>
                                </list>
                            </field>
                        </page>
                        <page string="Các khoản khác" name="inputs">
                            <div class="alert alert-info" role="alert">
                                <strong>💡 Hướng dẫn:</strong> Nhập các khoản bổ sung:
                                <ul>
                                    <li><b>PERFORMANCE</b> - Tổng lương năng suất (hệ thống tự tính theo công thực tế)</li>
                                    <li><b>BONUS</b> - Thưởng</li>
                                    <li><b>DEDUCTION</b> - Phạt</li>
                                    <li><b>ADVANCE</b> - Tạm ứng</li>
                                </ul>
                                <p class="mb-0"><i>VD: Nhập PERFORMANCE = 5,000,000 với 20/22.5 công → Nhận 4,444,444</i></p>
                            </div>
                            <field name="input_line_ids">
                                <list editable="bottom">
                                    <field name="name"/>
                                    <field name="code"/>
                                    <field name="amount" sum="Tổng"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_hr_payslip" model="ir.actions.act_window">
        <field name="name">Phiếu lương</field>
        <field name="res_model">hr.payslip</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
