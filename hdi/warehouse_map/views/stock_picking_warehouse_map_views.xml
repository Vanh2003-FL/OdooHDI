<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Thêm nút gán vị trí vào view picking form -->
    <record id="view_picking_form_add_warehouse_map" model="ir.ui.view">
        <field name="name">stock.picking.form.warehouse.map</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <!-- Thêm nút gán vị trí sau trường quantity của move_line_ids -->
            <xpath expr="//field[@name='move_ids_without_package']//field[@name='quantity']" position="after">
                <button name="action_assign_warehouse_map_position" type="object" 
                        string="📍 Gán vị trí"
                        title="Gán vị trí trên sơ đồ kho"
                        invisible="parent.picking_type_code != 'incoming' or parent.state == 'done'"
                        class="btn btn-sm btn-link"/>
            </xpath>
        </field>
    </record>
</odoo>
