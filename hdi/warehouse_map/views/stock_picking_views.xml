<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Override stock.picking form để thêm nút gán vị trí -->
    <record id="view_picking_form_warehouse_map" model="ir.ui.view">
        <field name="name">stock.picking.form.warehouse.map</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <!-- Thêm nút vào header cho phiếu nhập kho -->
            <xpath expr="//button[@name='button_validate']" position="before">
                <button name="action_open_wizard_assign_positions" 
                        type="object" 
                        string="🏢 Gán vị trí" 
                        class="btn-info"
                        invisible="picking_type_code != 'incoming' or state not in ['assigned', 'waiting']"
                        help="Gán vị trí sản phẩm/Lot từ sơ đồ kho"/>
            </xpath>
        </field>
    </record>
</odoo>
